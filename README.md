## Abstract
  Our project focused on solving the classification problem we’d
  Implemented three machine learning algorithms that resulted in three
  accuracy values:
  
    - 0.58 (Logistic Regression)
    - 1.00 KNeighborsClassifier
    - 0.92 Algorithm (RandomForestClassifier)
  We used a fruit dataset called (Fruit Data with Colors).
## Introduction
  As we mentioned in the abstract above, what the project is about and what we did. We used a simple dataset in our work for the task of training a classifier to distinguish between different types of fruits. It contains 59 rows and 7 columns. In short, We created a model as we learned in the lectures and used the algorithms that were mentioned in the abstract.
The purpose of this project is to identify the machine learning algorithm that is best-suited for the problem at hand; thus, we want to compare different algorithms, selecting the best- performing one in terms of accuracy. Our objective is to learn a model that has a good generalization performance. Such a model maximizes the prediction accuracy.
## Related work or Background
  Fresh-Rotten-Fruits-Predictor .They create a TensorFlow model to predict the fresh and rotten fruits. By doing so, we hope we can easily separate fresh and
  rotten fruits from fruit images. Training Data and Testing Data that were used are sourced from Kaggle :https://www.kaggle.com/sriramr/fruits-fresh-and-rotten-for-classification
